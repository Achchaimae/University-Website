create database University;
use University;
create table Course(
    CourseID int primary key,
    CourseName varchar(50),
    CourseTeacher varchar(50),
    CourseTime varchar(50),
    CoursePlace varchar(50),
    CourseFile varchar(50)
);


